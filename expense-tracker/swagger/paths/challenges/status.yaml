get:
  tags: [Users] 
  summary: 특정 유저의 도전과제 현황 조회 (본인/관리자)
  description: 일반 사용자는 본인의 현황만, 관리자는 모든 유저의 현황을 조회할 수 있습니다.
  security:
    - cookieAuth: []
    
  parameters:
    - $ref: '../../components/parameter.yaml#/parameters/UserId'
  responses:
    '200':
      description: 조회 성공
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../../components/schema.yaml#/schemas/UserChallengeStatus'