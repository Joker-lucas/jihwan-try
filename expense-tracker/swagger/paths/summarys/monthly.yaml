get:
  tags: [Summary]
  summary: 월별 수입/지출 요약 조회
  description: 메뉴의 '이달의 가계'에 필요한 총수입, 총지출 합계를 조회.
  security:
    - cookieAuth: []
  parameters:
    - $ref: '../../components/parameter.yaml#/parameters/Year'
    - $ref: '../../components/parameter.yaml#/parameters/Month'
  responses:
    '200':
      description: 요약 정보 조회 성공
      content:
        application/json:
          schema:
            $ref: '../../components/schema.yaml#/schemas/MonthlySummaryResponse'